<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		 xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>com.github.bbottema</groupId>
		<artifactId>standard-project-parent</artifactId>
		<version>1.0.42</version>
	</parent>

	<artifactId>clustered-object-pool</artifactId>
	<packaging>jar</packaging>
	<name>clustered-object-pool</name>
	<version>4.0.3</version>
	<description>Clustered and keyed generic object pools</description>
	<url>https://github.com/bbottema/clustered-object-pool</url>
	<inceptionYear>2019</inceptionYear>

	<properties>
		<automaticModuleName>org.bbottema.clusteredobjectpool</automaticModuleName>
		<generic-object-pool.version>2.4.2</generic-object-pool.version>
		<license.owner.name>Benny Bottema</license.owner.name>
		<license.owner.email>benny@bennybottema.com</license.owner.email>
	</properties>

	<scm>
		<connection>scm:git:git://github.com/bbottema/clustered-object-pool.git</connection>
		<developerConnection>scm:git:git@github.com:bbottema/clustered-object-pool.git</developerConnection>
		<url>https://github.com/bbottema/clustered-object-pool</url>
	</scm>

	<issueManagement>
		<system>GitHub Issues</system>
		<url>https://github.com/bbottema/clustered-object-pool/issues</url>
	</issueManagement>

	<distributionManagement>
		<snapshotRepository>
			<id>central</id>
			<url>https://central.sonatype.com/repository/maven-snapshots/</url>
		</snapshotRepository>
		<repository>
			<id>central</id>
			<url>https://central.sonatype.com</url>
		</repository>
	</distributionManagement>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-jar-plugin</artifactId>
				<version>3.4.2</version>
				<configuration>
					<archive>
						<manifestEntries>
							<Automatic-Module-Name>${automaticModuleName}</Automatic-Module-Name>
						</manifestEntries>
					</archive>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-antrun-plugin</artifactId>
				<version>3.1.0</version>
				<executions>
					<execution>
						<id>verify-automatic-module-name</id>
						<phase>verify</phase>
						<goals>
							<goal>run</goal>
						</goals>
						<configuration>
							<target>
								<delete dir="${project.build.directory}/module-name-verification" quiet="true" />
								<unzip src="${project.build.directory}/${project.build.finalName}.jar"
									   dest="${project.build.directory}/module-name-verification">
									<patternset>
										<include name="META-INF/MANIFEST.MF" />
									</patternset>
								</unzip>
								<loadfile property="packagedManifest"
										  srcFile="${project.build.directory}/module-name-verification/META-INF/MANIFEST.MF" />
								<fail message="Packaged JAR is missing Automatic-Module-Name: ${automaticModuleName}">
									<condition>
										<not>
											<contains string="${packagedManifest}"
													  substring="Automatic-Module-Name: ${automaticModuleName}" />
										</not>
									</condition>
								</fail>
								<unzip src="${settings.localRepository}/com/github/bbottema/generic-object-pool/${generic-object-pool.version}/generic-object-pool-${generic-object-pool.version}.jar"
									   dest="${project.build.directory}/module-name-verification/generic-object-pool">
									<patternset>
										<include name="META-INF/MANIFEST.MF" />
									</patternset>
								</unzip>
								<loadfile property="genericPoolManifest"
										  srcFile="${project.build.directory}/module-name-verification/generic-object-pool/META-INF/MANIFEST.MF" />
								<fail message="generic-object-pool ${generic-object-pool.version} is missing Automatic-Module-Name: org.bbottema.genericobjectpool">
									<condition>
										<not>
											<contains string="${genericPoolManifest}"
													  substring="Automatic-Module-Name: org.bbottema.genericobjectpool" />
										</not>
									</condition>
								</fail>
							</target>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.sonatype.plugins</groupId>
				<artifactId>nexus-staging-maven-plugin</artifactId>
				<extensions>false</extensions>
				<configuration combine.self="override">
					<skipNexusStagingDeployMojo>true</skipNexusStagingDeployMojo>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.sonatype.central</groupId>
				<artifactId>central-publishing-maven-plugin</artifactId>
				<version>0.9.0</version>
				<extensions>true</extensions>
				<configuration>
					<publishingServerId>central</publishingServerId>
					<centralSnapshotsUrl>https://central.sonatype.com/repository/maven-snapshots/</centralSnapshotsUrl>
					<autoPublish>true</autoPublish>
					<waitUntil>published</waitUntil>
				</configuration>
			</plugin>
		</plugins>
	</build>

	<dependencies>

		<!-- core dependencies -->
		<dependency>
			<groupId>com.github.bbottema</groupId>
			<artifactId>generic-object-pool</artifactId>
			<version>${generic-object-pool.version}</version>
		</dependency>

		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<version>1.18.2</version>
			<scope>provided</scope>
		</dependency>
    </dependencies>
</project>
